#ifndef CHECKER_HPP
# define CHECKER_HPP

# include "Word.hpp"

// Class Checker
// bool is_answer_found(Word word1, Word word2)

class Checker {
	public:
		static bool is_answer_found(Word* word1, Word word2);
};

#endif
